name: Frontend CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: BACKEND
    steps:
      - uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/tingeso-frontend:latest
          build-args: |
            VITE_TRAVELAGENCY_BACKEND_SERVER=${{ vars.VITE_TRAVELAGENCY_BACKEND_SERVER }}
            VITE_TRAVELAGENCY_BACKEND_PORT=${{ vars.VITE_TRAVELAGENCY_BACKEND_PORT }}
            VITE_TRAVELAGENCY_KEYCLOAK_SERVER=${{ vars.VITE_TRAVELAGENCY_KEYCLOAK_SERVER }}
            VITE_TRAVELAGENCY_KEYCLOAK_PORT=${{ vars.VITE_TRAVELAGENCY_KEYCLOAK_PORT }}